<Window x:Class="MonitorBot.App.Views.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">
    <Border CornerRadius="8" Background="#1E1E2E" BorderBrush="#3A3A5C" BorderThickness="1"
            Padding="16,12" MinWidth="300" MaxWidth="400">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="13" FontWeight="Bold"
                       Foreground="#FFFFFF" Margin="0,0,0,6" TextWrapping="Wrap"/>
            <TextBlock x:Name="MessageText" FontSize="12"
                       Foreground="#BBBBCC" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</Window>
