<UserControl x:Class="MonitorBot.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <!-- Page header -->
        <Border DockPanel.Dock="Top" Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1"
                Padding="24,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Activity Log" Style="{StaticResource TextH1}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox Width="200" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Search logs..." Margin="0,0,8,0"/>
                    <Button Content="Refresh" Style="{StaticResource SecondaryButton}"
                            Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                    <Button Content="Clear" Style="{StaticResource DangerButton}"
                            Command="{Binding ClearCommand}" Margin="0,0,8,0"/>
                    <Button Content="Copy All" Style="{StaticResource SecondaryButton}"
                            Command="{Binding CopyAllCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Log table -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource BgPanel}">
            <DataGrid ItemsSource="{Binding Entries}"
                      IsReadOnly="True"
                      FontFamily="Consolas"
                      FontSize="12"
                      Background="Transparent"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TIME"
                                        Binding="{Binding Timestamp, StringFormat={}{0:HH:mm:ss.fff}}"
                                        Width="90"/>
                    <DataGridTemplateColumn Header="LEVEL" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3" Padding="6,2" HorizontalAlignment="Left" Margin="12,0">
                                    <TextBlock Text="{Binding Level}"
                                               FontSize="10" FontWeight="SemiBold"
                                               Foreground="{Binding Level, Converter={StaticResource LevelToColor}}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="130"/>
                    <DataGridTextColumn Header="MESSAGE"  Binding="{Binding Message}"  Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
